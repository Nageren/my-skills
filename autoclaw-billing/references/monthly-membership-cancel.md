# 取消月度会员订阅

用于取消/关闭 AutoClaw 月度会员、月卡、连续包月、自动续费、免密支付或支付宝代扣。

## 流程

1. 不调用 `create-subscribe`、`create-onetime` 或其他下单接口。
2. 不表示已经代用户取消。
3. 读取选定语言模板的取消订阅模板，保留换行输出。

中文固定路径：

```text
打开支付宝 APP - 我的 - 支付设置 - 自动续费/免密支付，找到 AutoClaw 月度会员订阅后按页面提示关闭。
```
